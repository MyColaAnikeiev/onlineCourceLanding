import { DragScrollLeftDirective } from './drag-scroll-left.directive';

describe('DragScrollLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new DragScrollLeftDirective();
    expect(directive).toBeTruthy();
  });
});
